// GLRenderer.js

import * as glm from 'gl-matrix';
import alfrid, { GLTool } from '../lib/alfrid';

class GLRenderer extends GLTool {

    constructor() {
        super()
    }

    draw(mMesh, drawingType) {

    }
}

let GL = new GLRenderer();

export default GL;